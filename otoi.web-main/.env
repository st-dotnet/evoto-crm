# VITE_APP_API_URL=http://127.0.0.1:5000/api
VITE_APP_API_URL=http://localhost:5000/api
VITE_APP_NAME=OTOI
VITE_APP_VERSION=1.0.0.1



1) Configured database seeding so all required master data is populated using a single unified seed process.

2) Updated and aligned CLI seed commands to ensure consistent database seeding behavior across all commands.

3) Debugged and validated the item creation flow, and successfully verified that the /api/items API saves and fetches data correctly.

Link : https://github.com/st-dotnet/evoto-crm/pull/9


18-12-2025

End of Day (EOD) – Simple Summary (10:58 PM)

Fixed the dialog footer so it stays visible at the bottom while scrolling.

Improved Sales & Purchase Price inputs by placing the “With Tax / Without Tax” dropdown neatly beside the amount field.

Made “Advance Details” text bold and added a red star to “Basic Details” to show it’s required.

Set dropdown background to light grey for a cleaner look.

Added a placeholder section for “Custom Fields” with a message and create button.



18-12-2025 (CustomFields.tsx)
import React from "react";
import { useFormik } from "formik";

interface ICustomFieldsProps {
    formik: ReturnType<typeof useFormik>;
}

export default function CustomFields({ formik }: ICustomFieldsProps) {
    return (
        <div className="border rounded-lg p-4 flex flex-col items-center justify-center">
            <div className="text-center py-8">
                <div className="mb-4">
                    <img
                        src="https://via.placeholder.com/50"
                        alt="No custom fields"
                        className="mx-auto mb-2"
                    />
                    <p className="text-sm text-gray-500">
                        You don't have any custom fields created yet
                    </p>
                </div>
                <button
                    type="button"
                    className="px-4 py-2 text-sm text-blue-600 border border-blue-600 rounded hover:bg-blue-50"
                >
                    + Create Custom fields
                </button>
            </div>
        </div>
    );
}
